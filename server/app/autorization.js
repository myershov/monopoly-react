// var passport = require('passport')
// var Strategy = require('passport-local').Strategy
// var db = require('./db')

module.exports = app => {
  app.post('/login', (req, res) => {
    res.send({})
  })
}
